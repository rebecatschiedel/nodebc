let height = 10;
let base = 4;

let perimeter = 2 * (base + height);
let area = base * height;

console.log(`The perimeter of a rectangle is calculated by adding its base and height and multiplying it by 2. If the base is ${base} and the height is ${height}, the perimeter will be ${perimeter}.`);
console.log(`The area of a rectangle is calculated by multiplying its base and height. If the base is ${base} and the height is ${height}, the area will be ${area}.`);
